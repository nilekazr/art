<h2>Favourites</h2>

<h3></h3>
<% for (let i = 0; i < favourites.length; i ++) { %>  
    <div class="cell large-3">
      <img src="<%= favourites[i].image %>" alt="">
      <%= favorites[i].artistName %> 
    </div>
    <% } %>
